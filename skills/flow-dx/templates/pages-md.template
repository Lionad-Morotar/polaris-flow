<!--
  PAGES.md 模板（flow-dx Agents.md 切片产物，仅 pagesMd.applicable 项目生成）。
  目标路径：<repo-root>/.planning/codebase/PAGES.md

  用法：
  1. 按盘点 pagesMd.pkgs 的信号形态选表：前端页面族（路由表）或 CLI 命令族
     （命令表，替代表头见文件尾）；两族都有则分节都写
  2. refreshed 时间戳每次刷新必更新——它是下游判断清单新鲜度的唯一依据
  3. 数据来源段写清静态提取口径（哪个目录、哪几个字段）与差异标注约定，
     让阅读者能复核清单从哪来、何时过期
  4. 总览聚合到模块/命令粒度，明细逐子页面/子命令；只列真实存在的入口，
     禁止虚空引用；无静态注册补充段则整段删除
-->

# 页面与入口清单

<!-- refreshed: {YYYY-MM-DD} -->

数据来源：{提取口径，如「`src/router/routes/modules/*.ts` 的 `path` × `meta.title` 静态提取；路由前缀与文件名不一致的已标注」；文件系统路由项目写「`pages/` 目录枚举，动态参数以 `:param` 表示」}。

## 模块总览

| 路由前缀 | 模块 | 视图目录 | 路由文件 | 子页面数 |
| -------- | ---- | -------- | -------- | -------- |
| `/xxx`   | xxx  | `views/xxx/` | `xxx.ts` | n      |

{不走路由模块的静态注册入口在此用一段文字补充，如登录页、调试页及其生产隔离说明；无则删除本段}

## 模块明细

### {模块名}（{路由文件}）

{可选说明行：路由前缀与文件名差异、菜单分组等}

| 子页面 | 路径 |
| ------ | ---- |
| xxx    | `/xxx/yyy` |

<!--
  ═══════════════ CLI 命令族替代表头（pagesMd.pkgs 含 cli 信号时使用）═══════════════

  ## 命令总览

  | 命令 | 实现文件 | 子命令数 | 说明 |
  | ---- | -------- | -------- | ---- |
  | `xxx` | `src/commands/xxx.ts` | n | xxx |

  ## 命令明细

  ### {命令名}（{实现文件}）

  | 子命令 | 用法 | 说明 |
  | ------ | ---- | ---- |
  | `xxx yyy` | `xxx yyy <args>` | xxx |

  单命令 CLI（bin 为 string）退化为一张表：命令、用法、flags 说明，不开明细节。
-->
