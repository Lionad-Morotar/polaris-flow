# Phase 3 — 补漏（仅 xhigh）

以持已有清单的新审查者身份，在当前上下文（不 spawn 子代理）重读 diff 与外围函数，**只找清单上没有的缺陷**。不重新推导、不重新确认已有条目——任务是补漏。

聚焦第一遍容易漏的：

- 被移动/提取的代码掉了 guard 或锚点
- 二线 footguns：dataclass 默认值只求值一次、`hash()` 不确定性、锁范围收缩、有副作用的 predicate 方法
- 测试 setup/teardown 不对称
- config 默认值翻转

产出 ≤8 个新候选，各命名一个清单外缺陷。无新发现则空跑——禁止凑数。
