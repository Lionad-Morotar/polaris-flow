# 本地敏感词黑名单 —— 复制本文件为 secret-scan.local.txt 后逐行填写。
#
# 契约:
#   - 每行一个字面量,按子串精确匹配(区分大小写),命中即阻断 push
#   - 空行与 # 开头的注释行被忽略
#   - 本文件是「格式模板」可以入库;secret-scan.local.txt 被 .gitignore 排除,
#     真实敏感词永远不入库——入库即公开
#
# 适合放什么(以下为占位示例,按需替换):
#   <11位手机号>           # 手机号,如 185 开头的真实号码
#   internal.example.com   # 内部域名
#   <真名或内部花名>        # 真名/内部花名
#
# 不适合放什么:通用秘钥形态(sk-、AKIA 等)——形态规则已在
# .githooks/gitleaks.toml 中,这里只放「具体到值」的词。
